/**
 * typefighter
 * @function multiplyBy
 * @input number, number
 * @output number
 */
function multiplyBy(num, multiple) {
  return num * multiple;
}

multiplyBy("hello", 2); // <- This should trigger a warning
